# Auto Prep PR

Non-interactive PR preparation. Delegates to the `auto-prep-pr` agent.

Pass the target branch as the argument to this command. If omitted, defaults to `main`.

Delegate this entire task to the `auto-prep-pr` agent with the argument provided (or no argument if none was given).
